# 三木歯科・小児歯科（miki-dental）コーディングルール

> このファイルは案件固有情報のみを記載する。全案件共通ルール（ユーザー手動編集の保持・Skill自動発動・SCSS規約サマリ・baseリポジトリ同期）は**グローバル `~/.claude/CLAUDE.md` が毎セッション自動ロード**されるため、ここに再掲しない。
> （新規案件開始時、START_PROJECT.md Phase 2-1 がこのテンプレートを案件ルートに `CLAUDE.md` としてコピーし、Phase 2-3 で実値に更新する）

## 概要
埼玉県川越市の歯科医院「三木歯科・小児歯科」コーポレートサイトのリニューアル。
日本小児歯科学会認定の小児歯科専門医が在籍。大人と子どもで待合室・診療室を分けた院内環境が訴求の中心。

**今回のコーディング対象は TOP と ABOUT（当院について）の2ページのみ**（他18ページはデザイン未FIX）。

## 環境情報
- ローカルURL: http://260813-miki-dental.local/
- WP管理画面: http://260813-miki-dental.local/wp-admin/（ID: `test` / PW: `testtest`）
- 静的プレビュー: http://localhost:3000/（`npx gulp` で BrowserSync 起動）
- サイト種別: WordPress（**静的HTML先行 → 後で wp-build でテーマ化**）
- デザインソース: Figma
- Figmaファイル: https://www.figma.com/design/edrm51jOsbYYhENg6c7ubk/★歯科HPリニューアル
- リポジトリ: https://github.com/kana-o/miki-dental
- コーディング実行: **`/static-code` スキルを使う**（ユーザー指定）

> 制作前チェックの分析結果は `.page-info/20260810_制作前チェック_三木歯科コーポレートサイト_分析レポート.md` が正。
> ページ構成・表記揺れ・意図しない誤差・アクセシビリティの検出結果はこのレポートを参照する。

## SCSS変数一覧（src/scss/global/_color.scss / _font-family.scss定義）

### カラー（global/_color.scss）

命名は「見た目の色名」ではなく**用途**で行う。**HEX 直書き禁止**。

- `$color-main`: #10355d（ブランド基調の濃紺。ヘッダー背景・本文文字）
- `$color-accent`: #145aa6（見出し・ボタン・罫線）
- `$color-accent-sub`: #1767be（特徴バッジ背景）
- `$color-cta`: #61b8ff（SP固定CTA背景）
- `$color-bg`: #fff（ページ／カードの基本背景）
- `$color-bg-sub`: #f2f2f2（セクション背景）
- `$color-bg-light`: #d8ecfd（淡い水色の面。ナビのドロップダウン／アコーディオン／料金表の1列目）
- `$color-bg-tint`: #dce3ea（サブ見出しの帯・カード面）
- `$color-instagram`: #a61490（Instagramバナーの枠）
- `$color-banner`: #100e48（推薦バナーの帯）
- `$color-text`: `$color-main`（本文文字色）
- `$color-text-inverse`: #fff（濃色背景上の白抜き文字）
- `$color-border`: `$color-accent`（罫線・区切り線）

> デザインに無い色が出たら、既存に寄せず**用途ベースの変数を追加**してから使う。
> Figma Variables は未定義（空）のため、値は生値から採取している。

> **`$color-bg-tint` は Figma の塗り値ではない**。カンプのサブ見出し帯・カード面は塗り `#f2f2f2` に
> Figma の **NOISE エフェクト**（`#145aa6` 20%）が重なっており、書き出し実測が `#dce3ea`。
> REST も MCP も塗り値しか返さないため、`$color-bg-sub` のまま実装するとページ背景と同色になり面が消える。
> 該当ノードはカンプ全体で `Frame 167`（帯）が114箇所ほかカード7箇所。

> 制作前チェックで「入力ミスの疑い」としていた `#100e48` / `#a61490` は、実データを追った結果
> **どちらもバナーの意匠色**だった（誤りではない）。`$color-banner` / `$color-instagram` として変数化済み。

### フォント（global/_font-family.scss）
- `$base-font`: "Noto Sans JP"（本文・UI）
- `$second-font`: "Zen Maru Gothic"（見出し・丸ゴシック）
- `$en-font`: "Noto Sans"（英数字）
- `$regular`: 400 / `$medium`: 500 / `$semi-bold`: 600 / `$bold`: 700

> Figma に `LXGW WenKai Mono TC`（繁体字中国語向け・2回）と `BIZ UDPMincho`（2回）が
> ごく少数あるが、日本語サイトでの使用意図が不明。**読み込まない**。遭遇したら上記3書体に寄せて報告する。

### レイアウト
- デザイン幅: PC = 1600px / SP = 375px
- `.inner` の `max-width`: rem(1080)（**基準**）
  - 900px のセクション（お知らせ・FAQ・記事系）は `.inner` に**ページ固有クラスを併記して絞る**
  - 980px のセクションも同様

### レスポンシブ方針（重要）

- **基本は PC / SP の2段階**で組む。TAB（`@include mq("tab")`）は**実際に崩れた箇所だけ**入れる。
- **SP は画面幅に応じてサイズが可変**（chugai / otomi と同じ方式）:
  - `rem($px)` が `clamp(0.7倍, $px/16 * 1rem * var(--rem-scale), 1.3倍)` を返す
  - `.main` に `container-type: inline-size`、SP時 `--rem-scale: calc(100cqi / 375px)`
  - → **375px デザインの値をそのまま `rem()` に入れれば画面幅に追従**する
  - **`position: fixed` の要素（header / SP固定CTA）は `.main` の外に置く**。
    `container-type` は fixed の包含ブロックになり、画面固定が壊れるため。
- ブレイクポイント: `sp2`=375 / `sp`=768 / `tab`=1024 / `pc`=1440（`global/_breakpoint.scss`）

## 既存共通コンポーネント

新規作成前に既存を確認し、再利用すること。

すべて `src/scss/module/_common.scss` に定義。**見た目のみを持ち、余白は持たない**
（上下の間隔はページ固有クラスを同じ要素に併記して制御する）。

| クラス名 | 用途 |
|---------|------|
| `.inner` | コンテンツ幅制限（max-width: rem(1080)） |
| `.pic` | `<picture>` をレイアウトから外す（`display: contents`）。**サイズ・位置指定は中の `<img>` に書く** |
| `.heading-lg` | セクション見出し（丸ゴシック・中央寄せ・PC 40px / SP 30px） |
| `.section-title` | セクション上部の英字ラベル |
| `.heading-band` | 下層ページの帯見出し（濃色背景・白抜き・28px） |
| `.heading-sub` | 下層ページのサブ見出し（薄グレー背景・24px） |
| `.text-body` | 本文テキスト（PC/SP共通 16px・行間1.8）。下層ページの説明文で最も多く使う |
| `.icon-check` | チェックマークアイコン（24×24・SVG背景） |
| `.marker` | 本文中の太字＋マーカー帯 |
| `.tag` | 見出し下の小見出しラベル |
| `.badge-list` / `.badge` | バッジ（駐車場完備 等） |
| `.more` | 「もっと詳しく」矢印リンク（`--light` で白抜き） |
| `.btn` | ボタン |
| `.cta` | 予約CTA（`parts/cta.html`）。求人ページも `.cta__tel-icon` 等を再利用する |
| `.logo` | ロゴ |
| `.page-head` | 下層ページのFV |
| `.breadcrumb` | パンくず |
| `.side-nav` | PC固定サイドナビ |
| `.sp-cta` | SP固定CTA |
| `.u-sr-only` | 視覚的に非表示・スクリーンリーダーには読ませる |
| `.br-sp` / `.tab-none` / `.sp-none` / `.tab-show` / `.sp-show` | 表示切替ユーティリティ |
<!-- Phase 3 で作成したものをここに追記していく -->

## 共通パーツ（src/html/parts/）

静的HTML段階では `@@include('parts/xxx.html')`（gulp-file-include）で読み込む。

| ファイル | 用途 |
|----------|------|
| `parts/header.html` | ヘッダー（PC: G-nav＋ドロップダウン3種 / SP: ハンバーガー） |
| `parts/footer.html` | フッター |
<!-- Phase 3 で作成したものをここに追記していく -->

## 自動更新ルール

共通コンポーネント新規作成時 → 上の「既存共通コンポーネント」テーブルに追記

## 案件固有の注意事項

### 実装方針（確定済み）

| # | 項目 | 決定 |
|---|---|---|
| 1 | `.inner` 基準幅 | 1080px。900/980 のセクションは個別クラスで絞る |
| 2 | ブレイクポイント | PC/SP の2段階が基本。TAB は崩れたときだけ追加 |
| 3 | SP固定CTA のコントラスト比 2.14:1（WCAG AA 不合格） | **デザイン通りに実装**（改善はクライアント判断） |
| 4 | お知らせ / スタッフ / FAQ | 静的段階は静的マークアップ。WP化時にお知らせのみ動的化 |

### FVスライダーは Swiper で実装する（ユーザー指定）

TOP FV の `スライダー`（PC 1797×392 / SP 1172×256・歯型マスクの画像5枚）。
`Marquee Animation　続き`（node `1137:5405`）という2周目用フレームが存在するため
デザイン上の意図は無限横スクロールだが、**実装は Swiper を使う**（ユーザー指定）。

- `loop: true` ＋ `autoplay`、`freeMode` 系で途切れない横流れにする
- 画像は歯型のマスク付き。マスクは SVG または `mask-image` で表現する

### PCグローバルナビのドロップダウンは3つ

`一般歯科` / `小児歯科` / `スタッフ` の3項目にドロップダウンがある
（レポートには一般歯科・小児歯科の2つとしか書かれていないが、Figma実データでは3つ）。

### 表記揺れ（レポート「表記の揺れ」より）

Figma のテキストをそのまま写すと揺れが混入する。**実装時は下記に寄せる**:

| 揺れ | 採用する表記 |
|---|---|
| お子様 / お子さま | 各ページのデザインの表記に従う（TOP=お子様、ABOUT=お子さま） |
| 患者様 / 患者さま | 同上（TOP=患者様、ABOUT=患者さま） |
| むし歯 / 虫歯 | **むし歯** |
| できる / 出来る | **できる** |
| 範囲記号 `～` / `〜` / `-` | 全角チルダ **`～`** |
| 全角数字 | **半角数字** |
| `TEL：` / `TEL : ` | **`TEL : `**（半角コロン＋前後スペース） |

> 揺れを直したら、修正箇所をユーザーに報告すること。

### 既知の誤字（デザイン側の不備）

- `a SP TOP`（1230:7748）の `患 向上心を持ったスタッフが` は脱字。
  PC版の `患者様との会話を大切にし、向上心を持ったスタッフがお待ちしております` が正しい。**PC版を採用する。**

### ダミーコンテンツ

- お知らせの `2026.04.15 ゴールデンウィークの休診` は全件同一のダミー。実データ未確定。

### 非表示ノード（hidden）は実装しない

`b PC ABOUT` の `お子さまの治療・健診は小児専門医が担当`、`Polygon 4`、`Union`、
TOP の `Group 7`（旧お知らせ枠）など。旧版の残骸のため**マークアップに含めない**。

### ABOUT は PC / SP でセクション構成に差異がある

| PC版 | SP版 | 対応 |
|---|---|---|
| 連携 | 診療体制 | 名称のみ違う可能性。実装時に確認する |
| カウンセリング | なし | SPで消える想定か要確認 |
| 予約・相談（900×172） | CTAインスタンス（330×260） | 別コンポーネントに差し替え |
| Frame 147（1080×426） | 宣言文（345×536） | 対応関係を実装時に確認 |
| サイド（固定サイド 60×399） | なし | SPでは非表示 |

> 該当セクションの実装時に、都度ユーザーへ確認すること。
